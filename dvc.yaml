stages:
  plot-data:
    cmd: matlab -noFigureWindows -batch "run('scripts/plot_data.m');"
    deps:
    - scripts/plot_data.m
    - data/raw/data.parquet
    outs:
    - figures/plot.png
    meta:
      calkit:
        type: figure
        title: The plot
        description: The plot.
